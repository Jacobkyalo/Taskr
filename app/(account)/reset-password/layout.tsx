import type { Metadata } from "next";
import Page from "./page";

export const metadata: Metadata = {
  title: "Taskr - Reset Password",
  description: "Your taskr reset password page",
};

export default Page;
